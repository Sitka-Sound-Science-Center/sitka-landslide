import Page from "/components/Page";
import Risk from "/components/Risk";
import ForecastRisk0 from "/content/ForecastRisk0.mdx";
import ForecastRisk1 from "/content/ForecastRisk1.mdx";
import ForecastRisk2 from "/content/ForecastRisk2.mdx";
import riskDefinitions from "/content/riskDefinitions";

export const page = {
  title: "How to prepare",
  description: "Learn what you and your community can do to be ready for a landslide",
};

export const style = { display: "flex", gridGap: "6px", alignItems: "center" };

### How can I reduce my risk?

Debris flows cannot be prevented, but landslide disasters can. Risk is a combination of hazard, exposure, and vulnerability. When hazard is elevated, decreasing exposure (by moving out of areas where landslides are likely to happen) is the best way to decrease your risk.

<ForecastRisk0 />

### Recommended actions

<h4 id={riskDefinitions[2].slug} style={style}>
  <span>
    <Risk riskLevel={2} hasText={false} />
  </span>
  <span>Preparing for high risk</span>
</h4>

<ForecastRisk2 />

<h4 id={riskDefinitions[1].slug} style={style}>
  <span>
    <Risk riskLevel={1} hasText={false} />
  </span>
  <span>Preparing for medium risk</span>
</h4>

<ForecastRisk1 />

export default ({ children }) => (
  <Page title={page.title} description={page.description}>
    {children}
  </Page>
);
